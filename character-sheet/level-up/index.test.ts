import { createDefaultOwner } from '../../actor2/index.ts'
import { resolveOwnerClass } from './index.ts'
import { describe, test, assert, expect } from 'vitest'

/* describe('resolveOwnerClass', () => {
    test('', () => {
        const owner = createDefaultOwner()
        const r = resolveOwnerClass(owner, 'fighter')

        // I guess this is the level that would exist?
        console.log('r', r)
    })
}) */